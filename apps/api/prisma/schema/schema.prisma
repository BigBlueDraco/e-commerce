generator client {
  provider = "prisma-client-js"
  previewFeatures  = ["prismaSchemaFolder"]
  // seed    = "./prisma/seed.ts"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

